# 审核报告 · 打卡与统计模块 (module-checkin)

> 审核对象：`production/staging/2026-08-08-打卡/`  
> 审核日期：2026-08-08  
> 审核人：req-reviewer  
> 审核阶段：Stage 1 需求审核（confirmed → dev-ready gate）  
> **更新：2026-08-08 — 阻塞项已闭合，修改已提交（a585edd）**

---

## 前置检查

| 检查项 | 结果 |
|--------|:--:|
| `requirement.md` 存在 | PASS |
| `epic-story.md` 存在 | PASS |
| `STATUS.md` 存在 | PASS |
| STATUS 状态为 `confirmed` | PASS |
| 关联已有文档路径有效 | PASS |

Gate 通过，进入正式审核。

---

## 1. 9 维度总览

| # | 维度 | 结论 | 严重度 |
|---|------|------|:--:|
| 1 | 动机合理性 | **通过** — 打卡模块 GWT 结构化是对已有 `module-checkin.md` 的验收标准补充，动机清晰。 | — |
| 2 | 范围完整性 | **通过（闭合后）** — 覆盖打卡时间窗口、撤销打卡、连续完成统计、完成率统计、数据看板、孩子端统计、订阅消息、结算机制 8 个功能域，27 个用户故事。 | — |
| 3 | 需求可验证性 | **通过** — 每条 US 采用 GWT 格式，含具体数值（百分比、天数、时间）和可判定结果。 | — |
| 4 | 场景覆盖 | **通过（闭合后）** — B1 已通过新增 US-CHK-27（撤销打卡与时序规则，5 个 GWT 场景）闭合。 | — |
| 5 | 边界与异常 | **通过（闭合后）** — 22 条边界场景覆盖 4 类。已补充 BE-20/BE-21/BE-22。 | — |
| 6 | 现状兼容性 | **通过（闭合后）** — 状态体系映射表（S5）已添加，订阅消息优先级已统一。Q10/Q11 待下游阶段确认。 | — |
| 7 | 跨变更一致性 | **通过** — 本月仅此一个 staging 需求变更，无并行冲突。 | — |
| 8 | 影响分析完整性 | **通过（闭合后）** — 跨家庭数据隔离声明已添加（S6）。 | — |
| 9 | 优先级合理性 | **通过（闭合后）** — B2（订阅消息统一 Should）、B3（徽章检测移除）已闭合。 | — |

---

## 2. 问题清单

### 阻塞项（已闭合）

**~~B1~~ 撤销打卡与时间窗口规则/结算机制的交互场景缺失** [维度 4 / 6]

> ✅ 已闭合。新增 US-CHK-27 撤销打卡与时序规则，覆盖：结算前正常撤销、结算后不可撤销（终态）、撤销后重新打卡受时间窗口约束、撤销与结算竞态（23:59:50 撤销 → 00:05 结算以最终状态为准）、撤销后逾期不可逆。

**~~B2~~ 订阅消息优先级内部矛盾及与已有文档冲突** [维度 9 / 6]

> ✅ 已闭合。订阅消息 CHK-22~25 全站统一为 Should/二期，与 `index.md` §9 和 `module-checkin.md` §1.1 一致。

**~~B3~~ US-CHK-26 结算任务包含徽章检测但徽章功能不在 Must 范围** [维度 9]

> ✅ 已闭合。从 US-CHK-26 移除徽章解锁检测，结算任务仅保留状态变更 + 连续天数更新。徽章检测逻辑并入二期 CHK-ST-05。

### 建议项（已采纳）

**~~S4~~** ✅ US-CHK-01 补充了按钮文案按类型区分引用。  
**~~S5~~** ✅ §7 新增"视觉状态 ↔ 业务状态映射表"。  
**~~S6~~** ✅ §8 非功能需求新增跨家庭数据隔离声明。  
**~~S7~~** ✅ 新增 BE-20（撤销竞态）、BE-21（鉴权失败）、BE-22（时间编辑后打卡有效性）。  
**~~S8~~** ✅ BE-07 已从 CHK-ST-01 覆盖范围移除。  
**~~S9~~** ✅ US-CHK-06 示例偏差从 11 分钟改为 2 分钟（23:59 vs 00:01）。

### 疑问项（待下游阶段确认）

| # | 问题 | 说明 |
|---|------|------|
| Q10 | 小学模式孩子端统计范围 | `module-display-mode.md` 规定小学模式仅本周完成率，staging 设计包含连续天数+徽章。实现时按展示模式文档为准。 |
| Q11 | 学龄前模式打卡撤销例外 | `module-display-mode.md` 决策#6 规定学龄前不做打卡撤销。实现时按展示模式文档为准，撤销按钮按展示模式控制可见性。 |

---

## 3. 三判决

| 判决维度 | 结果 | 说明 |
|----------|:--:|------|
| 需求质量 | **通过** | B1/B2/B3 已闭合 |
| 现状兼容性 | **通过** | 状态映射已添加，Q10/Q11 实现时按 `module-display-mode.md` 为准 |
| 审批建议 | **批准进入 dev-ready** | 3 个阻塞项已闭合，6 个建议项已采纳 |

---

## 4. 变更摘要

| 变更 | 内容 |
|------|------|
| 新增 US | US-CHK-27 撤销打卡与时序规则（5 个 GWT 场景） |
| 新增边界异常 | BE-20（撤销与结算竞态）、BE-21（鉴权失败）、BE-22（时间编辑后打卡有效性） |
| 优先级调整 | 订阅消息 CHK-22~25 由 Could 提升为 Should/二期 |
| 结算调整 | US-CHK-26 移除徽章检测（并入二期 CHK-ST-05） |
| 文档增强 | §3.A 补充按钮文案类型区分引用、§7 新增视觉↔业务状态映射表、§8 新增数据隔离声明 |
| 数据修正 | US-CHK-06 示例偏差 11min→2min；BE-07 从 CHK-ST-01 移除；总数 26US→27US、19BE→22BE |
